{
  "name": "image-optimizer",
  "description": "",
  "version": "0.0.1",
  "author": "Hai Bacti",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "%NODE_PATH%/.bin/cross-env NODE_ENV=development %NODE_PATH%/.bin/webpack",
    "release": "%NODE_PATH%/.bin/cross-env NODE_ENV=production %NODE_PATH%/.bin/webpack --progress --profile --colors",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {},
  "devDependencies": {
    "comlink": "^3.2.0"
  }
}
